# image cropping website.

- git clone git@github.com:arrancy/image-cropping-website.git
- cd image-cropping-website
- npm install
- cd backend
- mkdir uploads(if not present)
- set up your secrets and database.
- node index.js
